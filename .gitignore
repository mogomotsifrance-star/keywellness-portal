# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# local-only tooling state (never commit)
supabase/.temp/
.claude/settings.local.json

# local Claude Code settings - machine specific
.claude/

# node — test tooling only; the site itself has no build step
node_modules/

# browser test bundles built by tests/run-advance.sh and tests/run-rehab.sh
tests/.ar-browser.js
tests/.drp-browser.js
